import keyword # Las palabras reservadas no pueden ser usadas como nombres de variables
print(keyword.kwlist) # Muestra la lista de palabras reservadas en Python
print(len(keyword.kwlist)) # Muestra la cantidad de palabras reservadas en Python